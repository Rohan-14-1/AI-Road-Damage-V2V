/**
 * Backend URL configuration.
 *
 * LOCAL DEVELOPMENT:
 *   Set BACKEND_URL to "" (empty string) — the frontend will use
 *   window.location.origin, assuming backend runs on the same host.
 *
 * DEPLOYED (Vercel frontend + Render backend):
 *   Set BACKEND_URL to your Render backend URL, e.g.:
 *   const BACKEND_URL = "https://ai-road-damage-v2v.onrender.com";
 */

const BACKEND_URL = "";
